# Importer la bibliothèque NumPy.
import numpy as np

# Créer les tableaux NumPy.
tableau1 = np.array([[1, 2], [3, 4]])
tableau2 = np.array([[5, 6]])
tableau3 = np.concatenate(([tableau1,np.array(tableau2).T]),axis=1)
#print(np.array(tableau2).T)
print(tableau3)